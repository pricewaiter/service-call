{
  "name": "service-call",
  "version": "0.8.0",
  "description": "DNS service discovery and HTTP request helper.",
  "main": "src/index.js",
  "scripts": {
    "coverage": "./node_modules/istanbul/lib/cli.js cover ./node_modules/mocha/bin/_mocha -- -R spec --exit --timeout 10000 ./test/*",
    "lint": "eslint src test",
    "test-mocha": "mocha -t 10000 --exit test",
    "test-travis": "npm run lint && npm run coverage",
    "test": "npm run lint && npm run test-mocha"
  },
  "repository": {
    "type": "git",
    "url": "github.com/pricewaiter/service-call"
  },
  "engines": {
    "node": ">= 4.0.0"
  },
  "keywords": [
    "service",
    "discovery",
    "dns",
    "consul",
    "http",
    "json",
    "microservice"
  ],
  "author": "PriceWaiter",
  "license": "MIT",
  "devDependencies": {
    "chai": "^4.2.0",
    "coveralls": "^3.0.4",
    "eslint": "^5.16.0",
    "eslint-config-airbnb-base": "^13.1.0",
    "eslint-config-prettier": "^4.3.0",
    "eslint-plugin-import": "^2.17.3",
    "express": "^4.17.1",
    "istanbul": "^0.4.5",
    "mocha": "^6.1.4",
    "prettier": "^1.18.2"
  },
  "dependencies": {
    "debug": "^4.1.1",
    "request": "^2.88.0",
    "retry-promise": "^1.0.0"
  }
}
